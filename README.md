
  # Create system from image

  This is a code bundle for Create system from image. The original project is available at https://www.figma.com/design/KEq4MbUvIVwYUFt6O0LRZL/Create-system-from-image.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  